// Modern Gradle (AGP 8+) — plugins are resolved via settings.gradle pluginManagement.
// This file can stay minimal. No buildscript/classpath or repositories are needed here.

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
